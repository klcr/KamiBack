{
  "name": "@ocr-mapping/web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "check": "biome check ./src",
    "format": "biome format --write ./src",
    "lint": "biome check ./src",
    "test": "vitest run"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0"
  }
}
